{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "build",
      "problemMatcher": ["$tsc"],
      "label": "npm: build",
      "detail": "tsc && mkdir -p dist/db && cp src/db/*.sql dist/db/"
    }
  ]
}
